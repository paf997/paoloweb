import React, { useState } from 'react';
import './App.css';

function Footer(props) {

    return(
      <header className={"footer"}>
        <a href="#">about_me()</a>
        <a href="#">my_work()</a>
        <a href="#">my_resume()</a>
      </header>
    )
}

export default Footer;